export interface UserTypes {
  name: string;
  email: string;
  password: string;
  role: string;
}
export interface TodoTypes {
  title: string;
  description: string;
  status: string;
  createdAt: Date;
}
